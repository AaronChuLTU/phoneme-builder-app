/**
 * Settings. Theme toggle (stored in a cookie) and layout preferences go here.
 */
export default function Settings() {
  return (
    <div className="flex flex-col gap-4">
      <h2 className="text-2xl font-bold">Settings</h2>
      <p className="text-[var(--text-muted)]">Theme controls coming next.</p>
    </div>
  );
}
