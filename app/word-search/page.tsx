/**
 * Word Search builder. Word list + grid preview + Generate button go here.
 */
export default function WordSearchBuilder() {
  return (
    <div className="flex flex-col gap-4">
      <h2 className="text-2xl font-bold">Word Search Builder</h2>
      <p className="text-[var(--text-muted)]">Builder form coming next.</p>
    </div>
  );
}
