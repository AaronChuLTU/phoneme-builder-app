/**
 * Wordle builder. Form + live preview + Generate button go here next.
 */
export default function WordleBuilder() {
  return (
    <div className="flex flex-col gap-4">
      <h2 className="text-2xl font-bold">Wordle Builder</h2>
      <p className="text-[var(--text-muted)]">Builder form coming next.</p>
    </div>
  );
}
